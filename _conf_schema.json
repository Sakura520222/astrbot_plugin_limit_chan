{
  "daily_limit": {
    "description": "全局默认每日限制次数",
    "type": "int",
    "default": 20,
    "hint": "所有用户和群组的默认每日 AI 使用次数限制"
  },
  "mode": {
    "description": "全局默认计数模式",
    "type": "string",
    "default": "individual",
    "options": ["individual", "shared"],
    "hint": "individual: 独立模式（每个用户独立计数）, shared: 共享模式（群组内共用计数）"
  },
  "blacklist": {
    "description": "黑名单用户ID列表",
    "type": "list",
    "default": [],
    "hint": "被禁止使用 AI 功能的用户ID列表"
  },
  "whitelist": {
    "description": "白名单用户ID列表",
    "type": "list",
    "default": [],
    "hint": "可以无限制使用 AI 功能的用户ID列表（优先级高于黑名单）"
  }
}
